# Lab 03-3: STR 前/后变基

## 实验目标

学习 STR 指令的前变基和后变基寻址模式。

## 编译与运行

```bash
make
make run
```

## 核心内容

### STR 前变基

```asm
str x6, [x2, #8]!
```

1. 先更新地址: `x2 = x2 + 8`
2. 再存储数据到新地址

### STR 后变基

```asm
str x6, [x2], #8
```

1. 先存储数据到当前地址
2. 再更新地址: `x2 = x2 + 8`

## 应用场景

- 内存填充
- 数组写入
- 结构体初始化